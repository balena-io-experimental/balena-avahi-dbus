{
  "name": "resin-avahi-dbus",
  "version": "1.0.0",
  "description": "Demo project to control Avahi over DBUS on a resin.io device",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "avahi-dbus": "^0.1.0",
    "dbus-native": "^0.2.0"
  }
}
